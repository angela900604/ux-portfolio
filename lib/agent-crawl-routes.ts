import routes from "@/content/agent/crawl-routes.json";

/** Routes the Ask Angela agent crawls for rendered case study + research content. */
export const AGENT_CRAWL_ROUTES = routes as readonly string[];
